name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  backend:
    name: Backend (ruff + mypy + pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Lint (ruff)
        run: ruff check agentforge tests

      - name: Type check (mypy)
        run: mypy agentforge

      - name: Tests (pytest, 全离线 Mock 模式)
        run: pytest tests -q --cov=agentforge --cov-report=term-missing:skip-covered

      - name: Offline eval pipeline smoke
        # 阈值与 agentforge/evals/gates.py 的 DEFAULT_GATES 同源；省略 --fail-under 即用默认门
        run: >-
          python -m agentforge.evals.runner retrieval
          --fail-under "recall@5=0.8"
          --fail-under "mrr=0.7"
        env:
          DATABASE_URL: "sqlite+aiosqlite:///./ci_eval.db"

  frontend:
    name: Frontend (tsc + vite build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
